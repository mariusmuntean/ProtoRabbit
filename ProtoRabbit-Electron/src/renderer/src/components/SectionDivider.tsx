import { Divider, DividerProps } from 'antd'

export const SectionDivider = (props: DividerProps) => {
  return <Divider {...props} style={{ marginTop: '0.3em', marginBottom: '0.3em', backgroundColor: 'darkslateblue' }} />
}
